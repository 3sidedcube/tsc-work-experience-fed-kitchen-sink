(function () {
  const init = () => {
    // Start Here!
    // Open Task two in the browser, and open the console log to see the message below!
    console.log("Hello I'm a console log 👋");

    // You try and console log something!
    //
    // Lets create an Object
    // Can your object include someone's first and last name (Person Object)
    // And then console log it
    //
    // Lets add the person's favourite colour
    // And then console log it
    //
    // Lets create an Array
    // Can you array include people's names
    // And then console log it
    //
    // Lets create an Array of Objects
    // Can you array include multiple people, use the Person Object you created above
    // And then console log it
    //
    // This is how we store data using JSON
  };

  document.addEventListener("DOMContentLoaded", init);
})();
