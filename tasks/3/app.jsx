/**
 * Task 3
 */

export function App() {
  const handleSubmit = e => {
    e.preventDefault();

    // Submit the Template Data here to the API
  };

  return (
    <form onSubmit={handleSubmit}>
      {/* Template Title */}

      {/* Template Text Question */}

      {/* (Extension) Add extra text type questions to the Form? */}

      {/* Submit Button for Form */}
    </form>
  );
}
